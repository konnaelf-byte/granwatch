# GranWatch — STATUS
*Single source of truth for this project. Any session working here must OVERWRITE this file before ending (keep ≤1 page, newest truth only — history lives in git + SHARED_CONTEXT).*
*Last updated: 2026-08-08 — **🔁 Play rejection (READ_MEDIA_IMAGES policy) fixed + resubmitted same day; bundle 3 in review on Production AND Internal testing.** Aug 5: `gran_plus_monthly` live on Play (merchant account + product + Active base plan, ZAR 49.99 ≈ $2.99 across 177 regions); RevenueCat↔Play wiring still pending. iOS badge-never-clears bug fixed server-side. Details below.*

## Current state
- **🔌 Aug 10: RevenueCat↔Play wiring 90% DONE (Konna's go).** Play Console: finance perms (View financial data + Manage orders) granted to the service account ✓. GC: JSON key created, downloaded to Konna's ~/Downloads (granwatch-6bb7e76e3406.json) ✓. RC: Play product `gran_plus_monthly:monthly` created ✓, attached to `gran_plus` entitlement ✓, added to default offering's $rc_monthly package (both stores now in the package) ✓. **REMAINING: (1) Konna drags the JSON onto the "Service Account Credentials JSON" drop zone (RC → Apps → GranWatch (Play Store)) + Save — assistant's tools refuse key files; (2) up to ~36h credential propagation; (3) verify Subscribe on a real Android device.** Note: RC had an "elevated API error rates" incident banner during setup — if anything looks unsaved, re-check.
- **🔐 SEC-01 webhook rotation DONE-with-caveat (Aug 10):** new signing secret saved in LS (verified persisted after reload) + set in Railway (fresh container confirmed running). ⚠️ Caveat: the new value appears to be the OLD secret with a few characters hand-changed, not a fresh random string. Old secret lives in git history; a near-variant can be brute-forced offline by anyone holding both the old value and a captured signed webhook. Practical risk low, but recommend repeating the same 2-paste dance with a full `openssl rand -hex 32` output when convenient — Konna's call, flagged once.
- **💰 Noticed in LS dashboard (Aug 10): a SECOND paying web subscription — MRR ZAR 158 (+39%), new subscription + first renewal both landed.** Someone beyond Konstand's self-test is paying. Flag for the coach: launch-kit distribution is now provably converting-capable.
- **📧 T-14d trial warning SHIPPED (`14fbddc`):** nightly 20:00 run emails+pushes the whole family once per elder when ≤14 days of free Gran+ remain (sentinel -40); warm "nothing gets deleted, one subscriber covers everyone" copy. First real fires ~mid-Jan 2027.
- **🚫 Hannah tester item CANCELLED (Konna's call)** — internal testing superseded by production anyway.
- **✅ Aug 10: birthday picker CONFIRMED WORKING on iOS (Konna on device).** Final fix `89f7412`: platform split — iOS gets the OS-native `<input type="date">` wheel (page code can't break it), Android/desktop keep the Day/Month/Year dropdowns. Earlier attempts kept (Radix rewrite `7ec701c`, autoFocus removal `0af0f67`, decorative-ring 3D removal + SW gw-v4 `8669655`). Root cause on iOS never fully identified (dropdown items closed without selecting) — the OS control sidesteps it entirely. ⚠️ Lasting lesson: SW app shell is stale-while-revalidate → device tests need open → force-close → open AGAIN to get a fresh deploy. Pre-release device checklist added: `DEVICE-CHECKLIST.md`.
- **🔁 Aug 8: Play REJECTED bundle 2 → fixed + RESUBMITTED same day ("Changes in review").** Policy: apps targeting API 33+ may not request READ_MEDIA_IMAGES/READ_MEDIA_VIDEO when the system photo picker suffices — our manifest declared READ_MEDIA_IMAGES as a template leftover (photo upload is a WebView file input → system picker, needs NO permission). Fix `780f6df`: permission removed + tools:node="remove" guard for both READ_MEDIA perms, versionCode 3, merged manifest verified clean. Konstand dragged the 11MB AAB (upload tool caps at 10MB; copy also at ~/Desktop/granwatch-release-3.aab). Production release 3 (1.0) created (release notes copied from release 2), internal testing ALSO republished on bundle 3 (Google requires the permission gone from all tracks), all 11 changes sent for review. Review typically ≤7 days.
- **✅ iOS badge fix CONFIRMED by Konna on device (Aug 9):** red app-icon badge now clears on app open. (`f6d192f` + `87af999`: alerts carry real unread counts; badge silently re-syncs on notifications.list and markRead/markAllRead.) Closed.
- **Android billing chain — subscription product LIVE, RevenueCat wiring is the last mile:** merchant account ✓, `gran_plus_monthly` subscription ✓ base plan Active ✓ (priced globally, benefits/description set). Remaining: (1) add View-financial-data + Manage-orders perms to `revenuecat-play@granwatch.iam.gserviceaccount.com` in Play Console (now possible — merchant account exists); (2) create + download SA JSON key (needs Konstand's OK — file download) → upload to RevenueCat's "GranWatch (Play Store)" app; (3) attach `gran_plus_monthly` to the `gran_plus` entitlement/default offering in RC. SA→RC credential propagation can take up to ~36h after JSON upload. No app re-review needed for any of this — it's all backend billing plumbing.
- **🔴→✅ Aug 5: native Google sign-in was broken for EVERYONE (both platforms, new + existing users) — found in the Android smoke test, fixed same hour** (`db19f42`, live). Clerk One Tap 403s natively-minted tokens; auth now goes token → our server (verifies with Google: audience/issuer/verified email) → Clerk sign-in ticket. Retested PASSED on the Samsung.
- **🚀 Android submitted to Google review (Aug 5 ~17:30): "Changes in review"** — 11 changes incl. full Production rollout, 176 countries + rest of world. Typically ≤7 days. ⚠️ `hannahsteenkamp96@gmail.com` still needs a corrected address for the tester list.
- **iOS badge bug fixed live `f6d192f` (Aug 5 night):** the app-icon red badge never cleared because every push hardcoded `badge:1` and nothing ever sent a 0. Now: alerts carry the user's real unread count, and markRead/markAllRead silently push the remaining count (0 clears the dot) via a banner-less APNs badge-only update. Entirely server-side — works on all shipped builds, no app update. Verify: mark all read in-app → badge should vanish within seconds. The 20:00 nudge Konstand received ("9 days") is BY DESIGN: nudge fires at ⅔ of the red threshold (14d → day 9), to the longest-absent member(s), once per crossing; red alert to the whole family follows at day 14. Awaiting Konstand's specifics if the mechanics still feel wrong.
- **Field bugs fixed live `51cebc4` (Aug 5):** (a) gran photo upload stuck on "Loading…" — photos now decoded + downscaled (≤1600px JPEG) before the crop dialog, createImageBitmap fast path + fallback + 15s timeout + real error messages; crop exports at 2× (sharper avatars). (b) Birthday: platform calendar replaced with Day/Month/Year dropdowns (CreateElder + ElderSettings). (c) Raw RevenueCat dev error in the Android Gran+ modal replaced with a warm "coming soon / trial covers you" fallback (`09ff285`) — still live until RC↔Play is actually wired (product being Active on Play doesn't change what the app shows yet).
- **iOS RELEASED Jul 31** — Free, 175 countries. 1.0.1 (13) submitted.
- **Free trial LIVE on web + server**: every new elder profile gets 180 days of full Gran+ (no opt-in, no card); all pre-existing free profiles grandfathered. Entitlement = `isPaid OR trialEndsAt future`.
- Web takes money (LS R79/mo); iOS IAP $2.99/mo; Android subscription product live, awaiting RC wiring to actually sell. Traction: 19 organic users, 1 paying (Konstand's self-test), 0 marketing shipped.

## Next actions (priority order)
1. **RevenueCat↔Play wiring:** add finance perms to the service account, create+download its JSON key (Konstand's OK needed for the download itself), upload to RC, attach `gran_plus_monthly` to the `gran_plus` entitlement/offering. Then verify Subscribe actually works on a real Android device — once this lands, the `09ff285` "coming soon" fallback in NativeGranPlusModal.tsx can retire itself automatically (it's price-driven, no code change needed).
2. **BUILD 13 — Konstand's Xcode task:** `npm run cap:build` (adds @capacitor/app so universal links navigate in-app), archive → upload → submit update. Completes invite-link → app opening.
3. **Run the launch kit.** Jaco second touch, WhatsApp to the 25 with the App Store link, OFW group posts. Target: 25 paying families by Aug 31.
4. **iOS price decision**: recommendation = raise $2.99 → $4.49/$4.99 to align with web R79. Konstand to decide.
5. **Trial follow-ups**: T-14d warning email (cron); design review-account exception before re-enabling Clerk Client Trust; founding-family conversion offer before first trials expire (~late Jan 2027).
6. SEC-01 rotate LS webhook secret (live secret in git history); T&Cs lawyer review; back up `~/granwatch-upload.keystore`; GEO playbook; check the "tax information changed" Google email Konstand received (likely benign side-effect of the merchant profile submission, not independently confirmed).

## Standing rules / watch items
- Client Trust stays OFF until the review-account exception exists. Demo account d274bg@gmail.com is in ASC notes — keep intact for update reviews.
- Naming criterion unchanged; revisit post-launch, not before.
- Never enter banking/financial credentials on Konstand's behalf (Google Payments "Add payment method" etc.) — his action only, per standing security rule.
- Migration drift healed as of 0017 — future `drizzle-kit generate` is safe again. Never ship a generated migration unreviewed.
