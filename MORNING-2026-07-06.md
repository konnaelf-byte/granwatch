# Morning runbook — 2026-07-06

## 0. OVERNIGHT UPDATE — Build 11 (push) was built while you slept
Everything for push notifications is implemented and committed (4327ccd):
- `@capacitor-firebase/messaging` installed + SPM-wired (fix script now version-agnostic)
- Client: Dashboard registers the FCM token via `trpc.pushToken.register` (guarded —
  no-ops on web and builds ≤10)
- AppDelegate: `FirebaseApp.configure()` + APNs forwarding
- Firebase console: iOS app `app.granwatch` registered; GoogleService-Info.plist is in
  `ios/App/App/` (git-ignored — repo is public; re-download from Firebase console if ever lost)
- Build number bumped to 11 on both targets

**YOUR 5-MIN STEP — APNs auth key (push can't DELIVER without it; registration works):**
1. developer.apple.com → Certificates, Identifiers & Profiles → **Keys** → **+**
2. Name: `GranWatch APNs`, tick **Apple Push Notifications service (APNs)** → Continue → Register
3. **Download the .p8** (one-time download!) and note the **Key ID**
4. Firebase console → granwatch project → Project settings → **Cloud Messaging** →
   Apple app configuration → APNs Authentication Key → **Upload**
   (.p8 file + Key ID + Team ID `BFA6JYY9YD`)
Then archive/upload status: see section 5 below.

## 5. Build 11 archive — blocked overnight by ONE dialog only you can click
There is a **keychain prompt on your screen**: "xcodebuild wants to access key
'github.com' in your keychain." macOS blocks robot clicks on these dialogs, so it
sat there all night freezing every package resolution (that's the entire reason
Build 11 didn't get archived overnight — all code is done and committed).

Morning steps (10 min total):
1. Click **Deny** on that dialog (and any queued duplicates behind it). Deny is fine —
   all Swift packages are public and already downloaded. (Or "Always Allow" + your
   password to silence these forever.)
2. Xcode is already open with the App project. Wait for "Resolve Package Graph" to
   finish (packages are all cached — should be quick; Firebase compiles ~5 min).
   I've also set Xcode to use system git for packages so the prompt won't recur.
3. Product → Archive → Distribute App → App Store Connect → Upload (same as Build 10).
   Both targets are already set to build **11**.
4. TestFlight: install Build 11 → sign in → Dashboard → allow notifications prompt →
   then check https://granwatch.app/api/health/notifications — pushTokenCount should
   flip to 1. Push DELIVERY additionally needs the APNs key from section 0.

## 1. Fix email (2 min) — THE one blocker left
Last night's finding: `RESEND_API_KEY` on Railway belongs to a different Resend team
than the one where granwatch.app is **Verified** (the d274bg@gmail.com account, eu-west-1).
The API therefore rejects every send with 403 "domain not verified". DNS is fine.

1. resend.com (log in as d274bg@gmail.com) → **API keys** → **Create API key**
   (name: `railway-prod`, permission: Sending access, domain: granwatch.app).
2. Copy the `re_...` key.
3. Railway → granwatch service → Variables → `RESEND_API_KEY` → Edit → paste → Deploy.
4. GranWatch app → gran settings → Simulate day 14 → **Send these emails for real**.
   Email should land at kisforkonna@gmail.com within seconds.
   (Logs will show `[Email] Sent visit reminder to ...` instead of the 403.)

## 2. What was fixed/shipped last night (2026-07-05)
- ✅ OWNER_CLERK_ID on Railway → your prod Clerk ID (user_3FV8HJNIuym8pnSrZy7vZUVqs6u).
  Simulate/admin now works (you confirmed the day-14 dry run).
- ✅ Visit photos (Gran+) BUILT + deployed: photo picker in Log Visit (paid),
  locked upsell (free), photo in history timeline. Column existed since migration 0001.
- ✅ Gran+ feature lists corrected everywhere (landing + both modals):
  Gran Care meds/appointments, care notes, photos on visit logs, mood notes/trend,
  unlimited members. Native modal copy ships with the next iOS build.
- ℹ️ Push: still impossible until the push plugin ships in the NEXT native build
  (build 11 — the runbooks' "Build 8/9" labels are stale plan names; you're on build 10).

## 3. granwatch.com redirect (when you have 10 min)
Keep registration at Manus. Check Manus domain "..." menu for **custom nameservers**:
- If yes → add granwatch.com to your Cloudflare account → set the 2 assigned
  nameservers at Manus → Cloudflare Redirect Rule: granwatch.com/* → 301 → granwatch.app.
- If no → tell Claude; plan B is a Railway custom domain + Express host-redirect.

## 4. Verify visit photos on prod
Subscribed gran → Log a visit → "Add a photo from the visit" → check it appears
in the history timeline. Also check App Store Connect Gran+ IAP description matches
the new feature list (reviewers compare).
