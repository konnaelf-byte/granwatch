# GranWatch — STATUS
*Single source of truth for this project. Any session working here must OVERWRITE this file before ending (keep ≤1 page, newest truth only — history lives in git + SHARED_CONTEXT).*
*Last updated: 2026-08-05 — **🤖 ANDROID STAGED TO THE LAST BUTTON.** Smoke test PASSED (Google auth fixed `db19f42`). Play setup COMPLETE: support@granwatch.app live (Cloudflare Email Routing → konnaelf@icloud.com), contact details published, production release built (bundle 2 (1.0), public release notes, 176 countries + rest of world). **Waiting only on Google's pre-review checks (~14 min) → then "Send for review" in Publishing overview = Konstand's go.** Also today: photo-upload hang + birthday-picker UX fixed live (`51cebc4`). iOS RELEASED Jul 31 (build 12); 1.0.1 (13) submitted.*

## Current state
- **🔴→✅ Aug 5: native Google sign-in was broken for EVERYONE (both platforms, new + existing users) — found in the Android smoke test, fixed same hour** (`db19f42`, live). Clerk One Tap 403s natively-minted tokens; auth now goes token → our server (verifies with Google: audience/issuer/verified email) → Clerk sign-in ticket. **RETEST PASSED on the Samsung — Google + email sign-in both work. Android smoke test PASSED.** Email sign-up verified working throughout.
- **Android status (Aug 5 PM):** ALL setup gates cleared. Email Routing ACTIVE: support@granwatch.app → konnaelf@icloud.com (MX/SPF verified propagated globally); Play contact details published (email + https://granwatch.app); setup checklist complete; Play header already shows "GranWatch — Visit Tracker". Production release DRAFTED: bundle 2 (1.0), user-facing release notes, all 176 countries + rest of world; only 2 benign warnings (deobfuscation/debug symbols). **ONE STEP LEFT: "Send for review" in Publishing overview after Google's pre-review checks finish — awaiting Konstand's explicit go.** ⚠️ `hannahsteenkamp96@gmail.com` REJECTED by Play validation — needs her corrected address for tester list.
- **Field bugs fixed live `51cebc4` (Aug 5):** (a) gran photo upload stuck on "Loading…" — photos now decoded + downscaled (≤1600px JPEG) *before* the crop dialog, createImageBitmap fast path + fallback + 15s timeout + real error messages; crop exports at 2× (sharper avatars). (b) Birthday: platform calendar (month-by-month back to 1950) replaced with Day/Month/Year dropdowns (CreateElder + ElderSettings).
- **iOS RELEASED Jul 31** — Free, 175 countries. 1.0.1 (13) submitted; Clerk log shows an Apple-IP sign-in 2:43 AM Aug 5 (review likely ran).
- **Free trial LIVE on web + server** (verified in prod bundle `index-DCQvAvA3.js`): every new elder profile gets **180 days of full Gran+** (no opt-in button, no card); migration 0017 grandfathered **all existing free profiles** incl. the 19 June families. Entitlement = `isPaid OR trialEndsAt future` (`server/entitlement.ts`); server aliases effective entitlement onto `isPaid` so **frozen native build 12 unlocks trials with zero client changes**. Expiry LOCKS, never deletes — warm "your data is safe" message. Subtle countdown badge (amber <30d) on Care tab, mood trend, visit photo, settings, modal; subscribe-anytime kills it (billing starts immediately, no day-banking).
- **Admin gifting live**: granwatch.app/admin → per-elder **Gift Gran+ / Revoke** (owner-admin only). For retirement-village/frail-care comps + family. Track comps in the launch-kit table — DB can't tell comped from paid.
- **Split-the-bill removed** (web): it divided the *displayed* price only — checkout always charged full R79 ("Join split — R39.50" actually billed R79 as a second subscription). One volunteer family member pays. Native bundle still shows split UI until build 13 (harmless since `a7ab3b9` decoupled it from payment).
- Web takes money (LS R79/mo); iOS IAP $2.99/mo; Android build 2 one smoke test from Production. Traction: 19 organic users, 1 paying (Konstand's self-test), 0 marketing shipped.

## Next actions (priority order)
1. **BUILD 13 — this week, Konstand's Xcode task (everything else is prepped):** run `npm run cap:build` (runs cap sync → adds the @capacitor/app plugin so universal links NAVIGATE in-app; deep-link handler + AASA `/api/og/invite/*` already live), then archive → upload → submit update (demo account intact in ASC notes). This completes invite-link → app opening (#1 field issue). Note: build-12 devices may briefly open the app without navigating once Apple's AASA CDN refreshes — build 13 closes that gap. Bundle Android smoke test → Production into the same push.
2. **Field-report batch SHIPPED live `9065510` (reaches iPhones instantly — native shell loads server.url):** any family member can now subscribe whenever the profile isn't actually paid, incl. during trial (native modal trial-aware); admins can leave/step down when another admin exists; invite-code regenerate moved away from Copy; og-default.png redrawn without legacy artwork. App Store search invisibility = normal indexing lag (24–48h+); re-check Monday before investigating.
3. **Run the launch kit.** Jaco second touch (was explicitly reserved for approval — trigger fired), WhatsApp to the 25 with the App Store link, OFW group posts per the join→observe→post rule. Target: 25 paying families by Aug 31 (trial changes the metric: count *activated families* now, paying conversions from month 6).
4. **🚀 ANDROID SUBMITTED (Aug 5 ~17:30): "Changes in review"** — 11 changes incl. full Production rollout, 176 countries. Google first-app review typically ≤7 days. Then re-add Hannah with corrected email.
4b. **Android billing (Play IAP) — chain in progress:** DONE: service account `revenuecat-play@granwatch.iam.gserviceaccount.com` created (GC project granwatch), Play Android Developer API enabled, SA invited to Play Console (view-app-info; finance perms pending merchant), RC "GranWatch (Play Store)" app created (app58a193551b), `VITE_REVENUECAT_ANDROID_KEY=goog_OVkaEUDbAaAerBoSVeQueBBRADZ` set in Railway + live in bundle. Native modal shows graceful "coming soon / trial covers you" fallback meanwhile (`09ff285`). **BLOCKED ON KONSTAND: create Google Payments merchant account** (Play Console → Monetize → Subscriptions → "Set up a merchant account"; banking details). THEN: (1) create `gran_plus_monthly` subscription product ($2.99-tier, base plan monthly); (2) add View-financial-data + Manage-orders perms to the SA; (3) create+download SA JSON key → upload to RC app (needs Konstand's OK for the download); (4) attach Play product to `gran_plus` entitlement/default offering in RC. No app re-review needed for any of this.
5. **iOS price decision**: recommendation = raise $2.99 → $4.49/$4.99 (≈R80–89) to align with web R79, NOT drop web to R49 (no price-resistance evidence, diaspora earns hard currency, trial does acquisition now). ASC config only, no review. Konstand to decide.
6. **Trial follow-ups**: T-14d warning email (cron); design the review-account exception before re-enabling Clerk Client Trust; decide founding-family conversion offer before first trials expire (~late Jan 2027).
7. SEC-01 rotate LS webhook secret (live secret in git history); T&Cs lawyer review; back up `~/granwatch-upload.keystore`; GEO playbook; DB check for Manus CloudFront `photoUrl` on Margaret's profile (Sophie confirmed on placeholder).

## Standing rules / watch items
- Client Trust stays OFF until the review-account exception exists. R79 LS self-test sub: safe to cancel post-approval, but keeping it keeps the demo family paid for future *update* reviews.
- Demo account d274bg@gmail.com is in ASC notes, member of Sophie + Opa Joe families (both Gran+ active) — keep intact for update reviews.
- Naming criterion unchanged (name must let you infer function); revisit post-launch, not before.
- Complete Manus independence (verified Jul 29); `~/Documents/Claude/_to_delete/` awaits Konstand's deletion.
- Migration drift healed: drizzle snapshots were stuck at 0014 (0015/0016 hand-written) — 0017's snapshot now matches reality; future `drizzle-kit generate` is safe again. Never ship a generated migration unreviewed.
