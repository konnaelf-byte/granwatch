# GranWatch — STATUS
*Single source of truth for this project. Any session working here must OVERWRITE this file before ending (keep ≤1 page, newest truth only — history lives in git + SHARED_CONTEXT).*
*Last updated: 2026-08-26 — **GranWatch's first REAL affiliate commission deal is live**: gesalfde.co.za (SA handmade gift/wellness supplier — body scrub, hand cream, gift hampers) added to `server/giftPartners.ts` for the "Send Gift" button, ZA gift category — 10% commission agreed direct with the supplier (GranWatch's first non-test-phase commission deal; entry id `gesalfde`, priority 110, outranks the Petal & Post test-phase gift link for ZA). Domain verified live via browser before wiring it in (real product catalog + genuine customer testimonials — reads as a legitimate small SA business, not a placeholder). Bonus bizdev detail: the supplier has ~17k Facebook followers and has agreed to post about GranWatch — a real organic-reach opportunity, possibly worth coordinating with the Facebook cover banner asset already published (flagging for Konna, not assuming). `tsc --noEmit` clean, committed, pushed — live in ~3 min via Railway, no app release needed. Open for Konna: capture the supplier's registered business name for the internal note if he has it, and whether/how to time the FB post with anything else in flight. Previous: 2026-08-25 (evening, Konna live) — **(latest) Birthday reminders now fire 5 days ahead (was 3) + copy nudges "send flowers or a gift if you can't be there" (`be6bf96`, pushed, Railway-deploying) — per a promising new gift-supplier conversation: 3 days too tight for shipping. Mother's/Father's Day reminders do NOT exist yet — offered as next build (country-aware dates), awaiting Konna's go. Note: commit accidentally included 6 harmless `android/.idea/` config files (created when Android Studio opened tonight) — checked, no secrets; can gitignore later.** Also this evening: Billing Library 8 build SHIPPED TO INTERNAL TESTING + Android badge bug fixed same session. (1) Konna reviewed + approved the BL8 commits → pushed to origin (`cbd5451..410b262`), web layer smoke-tested fine on his phone. (2) NEW BUG live-reported and fixed within the hour: Android launcher red badge never cleared (Samsung ties the dot to tray notifications, which nothing ever removed if the app was opened from its icon; iOS unaffected — it uses the server's silent APNs badge-sync). Fix in `client/src/main.tsx`: `FirebaseMessaging.removeAllDeliveredNotifications()` on every launch + foreground resume (`0f6ae30`, pushed, Railway-deployed, **Konna confirmed working on device**). (3) Signed AAB built via `./gradlew bundleRelease` (signing fully scripted through git-ignored `keystore.properties` — no Android Studio dialogs needed; JAVA_HOME must point at Android Studio's JBR since the shell has no system Java). (4) **versionCode 5 (1.0.1) uploaded + published to Play Console INTERNAL TESTING, live Aug 25 10:14 PM** — coach drove the whole console flow in Chrome, Konna dragged the 11.3MB AAB (file too big for the browser-bridge upload tool) and gave the explicit OK before Save-and-publish. Release notes mention billing update + badge fix. Standard 2 warnings only (no deobfuscation file / no native debug symbols — same as every prior release). **The Aug 31 Billing Library 8 deadline item is now DONE pending one final check: Konna installs the internal-testing build on his Samsung, confirms Gran+ shows a price and the app behaves, then Promote release → Production.** Previous: 2026-08-25 (overnight wrap-up, ~03:30 UTC, end of the 8h autonomous window) — **Final check: still nothing new from Awin, FloraQueen, or Tyson/DKOI — all three genuinely still pending, not just unchecked twice.** Awin: browser session still logged out at the sign-in page (correctly left untouched, no password on hand), zero Awin emails in Gmail. FloraQueen (b2b@floraqueen.com): zero reply. Tyson/DKOI: same single July 22 thread as before, no new message. Sent Konna a plain-language wrap-up via SendUserMessage covering the two real headline outcomes (FloraQueen outreach + Awin application submitted; Billing Library 8 upgrade code-complete) and the decisions waiting on him (review+push the 2 local commits, build+sign the AAB, Play Console upload before Aug 31, CJ Tax Information, Zoho Mail Lite call). Nothing further to add beyond the two prior overnight entries below — this session's autonomous window is now complete. Previous: 2026-08-24 (overnight check-in #1, ~23:30 UTC) — **Billing Library 8 upgrade DONE (code-complete, not yet built/submitted or pushed — awaiting Konna's review).** `@revenuecat/purchases-capacitor` 10.4.0→13.4.1 (satisfies the required ^13.1.7 floor for Google's Aug 31 2026 Billing Library 8 mandate). Pre-checked: GranWatch's only IAP product is the `gran_plus_monthly` subscription (not a consumable) — so v11+'s "consumed one-time purchases can't be restored" change is a non-issue. `minSdkVersion 24` and Capacitor 8 were already ahead of v13.1.7's new floor (minSdk 23, Capacitor V7+) — no gradle/manifest changes needed. `npm run check` (tsc) passed clean, zero errors in `iap.ts` or anywhere else. `npm run cap:build` (vite build + cap sync) succeeded; re-checked for the old cordova-plugin-purchases conflict pattern (root cause of the Aug 14 crash) — none found, native module list is clean, single RevenueCat entry. Bonus: v13's native SPM support means the custom `scripts/fix-revenuecat-spm.mjs` hack (written for v10.4.0's lack of native SPM) is now vestigial — left in place, harmless no-op, can be deleted in a cleanup pass. `versionCode` bumped 4→5 in `android/app/build.gradle` (`versionName` left at "1.0.1", pure compliance upgrade, no user-facing change). **3 commits made locally on `main` (`eb6f3a2`, plus a pnpm-lock.yaml refresh): package.json, package-lock.json, pnpm-lock.yaml, android/app/build.gradle, ios/App/CapApp-SPM/Package.swift. NOT pushed to origin, and no AAB has been built/signed/submitted to Play Console — both deliberately held back for Konna to review first rather than done autonomously.** Next when Konna's back: review the diff (`git log -3`), push if happy, then `pnpm run cap:android` → build a signed AAB in Android Studio → upload to Play Console (Internal Testing first, per the original rollout pattern) before the Aug 31 deadline. Previous: 2026-08-24 (overnight) — **Konna asleep, coach kept moving: FloraQueen direct outreach sent + Awin publisher application SUBMITTED, now starting Billing Library 8 upgrade.** (1) Emailed b2b@floraqueen.com from konstand@granwatch.app (sent via Zoho webmail, verified in Sent folder) — introduced GranWatch, noted FloraQueen isn't on CJ Affiliate, asked for the right partnership contact/program. Awaiting reply. (2) **Awin publisher account for Better Creation Pty Ltd: application SUBMITTED, status "Application Received" (pending review, most processed within minutes per Awin).** Konna created the login himself (email/password — assistant does not touch account creation/passwords, same hard rule as CJ tax fields); coach completed the rest: Promotional Type = Lead Generation (Content, primary); Promotional Space = granwatch.app + description; Sectors = Gifts & Flowers + Health & Beauty (Retail & Shopping); Verification address = Better Creation's COR14.3 registered address (7 Columbia Lane, Croydon Olive Estate, Somerset West, Western Cape, 7130), phone +27 82 456 9800. First submit attempt failed generically ("We couldn't complete your registration") — retried once, succeeded. **Once approved: search Awin's advertiser directory for FloraQueen AND Interflora UK (merchant 1969, confirmed live, claims "UK and worldwide" delivery — the most promising international-flower lead found so far, stronger than Teleflora/From You Flowers which are both CJ-confirmed ~96% US-only despite marketing international delivery separately).** (3) Checked Gmail for a Tyson/DKOI reply on the 28-July-call follow-up sent this morning — nothing yet, too early to expect one. (4) Pivoting next to the **Billing Library 8 upgrade (purchases-capacitor 10.4.0→13.x, versionCode 5)** — Aug 31 deadline, previously flagged as needing its own dedicated session; using tonight's open-ended window for it. Previous: 2026-08-24 (CJ Affiliate) — **CJ Affiliate registration underway for Better Creation Pty Ltd; FloraQueen affiliate plan hit a dead end, Teleflora ruled out too.** Network Profile description (985-char, GranWatch/FloraQueen pitch) written+saved; Account Information (org name "Better Creation Pty Ltd", address from COR14.3 cert, phone, ZAR currency) filled and saved — Konna typed phone/currency himself. **Tax Information / Payment Information NOT filled** — contains SA tax number + 2 directors' government ID numbers from the COR14.3 cert; assistant hard-blocked from entering financial credentials/gov IDs even under Konna's explicit authorization ("I give you permission... go") — this is a standing policy, not a one-off refusal; offered to narrate field-by-field mapping so Konna types it himself, not yet done. **Confirmed live in Konna's CJ account: FloraQueen is NOT on CJ Affiliate at all** (name search + "flower" keyword search across all 36 flower-category results, both empty) — the existing `giftPartners.ts` FloraQueen plan (12%/sale, flowers-abroad + flowers-israel links) has no real CJ program to register for; current links are plain/untracked placeholders, same as Petal & Post. **Investigated Teleflora as substitute (CJ id 5003654, 8-9% commission, strong EPC) — ruled out**: its own CJ advertiser page states "Serviceable Area: UNITED STATES" with 96.4% of tracked conversions from the US; From You Flowers (another strong CJ flower advertiser) shows the same pattern (96.3% US) — looks like a CJ flowers-category-wide US bias, not a Teleflora fluke. Note: Teleflora *does* market international delivery via a separate consumer site (international.teleflora.com) — but that is NOT the same as the CJ-tracked affiliate program, which is what would actually pay commission. **Next: either chase FloraQueen's real affiliate channel directly (b2b@floraqueen.com — Awin/VigLink lookups were inconclusive, not confirmed absent) or find a different CJ advertiser with genuine SA/international serviceable area, before swapping any tracking link into `giftPartners.ts`.** Not launch-blocking — flower links work today, just uncommissioned. Previous: 2026-08-24 (later) — **Send Flowers/Gift bug FIXED + deployed (`cbd5451`, live, bundle `index-CrEr0_6y.js`):** Konna reported his wife Inke's iPhone (iOS 18.7.8) did nothing on tapping Send Flowers, same gran (Ouma Ingrid) worked fine from his own phone — looked like a device/storage issue but wasn't. Root cause: `handleSendFlowers`/`handleSendGift` awaited the gift-log mutation BEFORE calling `window.open()`; on some iOS Safari versions the user-gesture token expires while the JS event loop is awaiting, so the popup gets silently blocked. Fix: open the partner link synchronously first, fire the log mutation unawaited after. `tsc --noEmit` clean. Also stood up a shared **GranWatch Marketing** Google Drive folder (d274bg@gmail.com account) so Konna, the new NL/BE regional distributor (chanispies@gmail.com, writer access confirmed), and Coach stay in sync on strategy/content — 6 subfolders (Brand Brief & Guidelines / Content Calendar / Content Drafts / Brand Assets / NL-BE Translations / Performance & Reports), with Docs for the real Brand Brief (from `BRAND.md`), Marketing Masterplan, Social Launch Pack, and Marketing Asset Pack (caught + fixed stale "families tell us" testimonial phrasing in the Asset Pack to match the already-corrected live site — same rule as the Aug 23 blog-copy pass). Populated Brand Assets with the app icon/logo, Play feature graphic, 5 social cards, and 2 QR codes by copying straight into the local **Google Drive Desktop sync mirror** (`~/Library/CloudStorage/GoogleDrive-d274bg@gmail.com/My Drive/GranWatch Marketing/...`) — this bypasses the Read-tool's ~25k-token base64 truncation ceiling entirely and is now the go-to method for any future image→Drive upload. Two raw/uncropped photoreal "Nanna" concept video frames (Artlist) were also placed in a clearly-labelled `NOT YET APPROVED — Raw Photoreal Nanna Reference` subfolder with a README — per standing brand rule, NOT usable anywhere public without Konna's explicit sign-off; the icon Gran (app icon/logo) remains the only official GranWatch mascot. Deliberately EXCLUDED from the shared folder: `STRATEGY.md`/`GLOBAL-STRATEGY.md` (valuation/exit-ladder content), the 2 PRIVATE supplier one-pagers, and the Rep Kit — judged too sensitive for an external non-equity marketing partner; flag to Konna to override any of these if he disagrees. Previous: 2026-08-24 — i18n fit pass (`7c47fb3`+`4965e36`, live): 62 over-long labels shortened across 7 locales + ElderProfile fixes from Konna's af screenshot (tighter icon gap/px-3 on primary buttons, copyLink→single verb all locales, af History tab "Verlede", fil Visits "Bisita"). ⏰ Billing Library 8 upgrade due before Aug 31 (purchases-capacitor 10.4.0→13.x, versionCode 5). FloraQueen commission: Konna to register at CJ Affiliate (12%/sale), then swap tracking links into giftPartners.ts. Previous: 2026-08-23 — Blog copy pass (`1b2c398`+`4ee3ff9`, verified live): Konna challenged "Families tell us the arguing stops…" — all 4 "families tell us" testimonial claims were unsupportable at ~40 users, rewritten as mechanism claims ("once everyone can see when Gran was last visited, there's nothing left to argue about"); mangled phrase "covering the family's tools" (×2) → "the bills". Rule logged: no invented testimonials in content — mechanism claims only until real quotes exist. Previous: 2026-08-20 — **🚀 GRANWATCH IS LIVE ON GOOGLE PLAY.** Submission 3 (versionCode 4 / 1.0.1) approved and PUBLISHED (IARC live-rating email Aug 20 was the tip-off; console confirms "Published"; public listing live at play.google.com/store/apps/details?id=app.granwatch). Both stores now live: iOS since Aug 11, Android since Aug 20. Same day: approved brand-true feature graphic (icon-512 gran, 1024×500) + 5 framed screenshots (1080×1920, headline captions, order 01→05) uploaded to the Default store listing, saved, and **submitted for review** (2 changes: Feature graphic + Phone screenshots — cosmetic, app stays live meanwhile). Konna's launch actions queued: Samsung install smoke test (sign-in, gran profile, log visit, Gran+ price — RevenueCat "coming soon" fallback should retire), create granwatchapp FB/IG + fire social Post 1, send the 10 WhatsApp launch-kit messages. Previous: 2026-08-18 — **📧 GRANWATCH EMAIL MIGRATED TO ZOHO MAIL, fully verified — but Mail.app desktop client BLOCKED by Zoho's free-plan limits (needs Konna's decision).** DNS cut over from Cloudflare Email Routing to Zoho (MX/SPF/DKIM), Zoho's own verifier confirmed all records pass; Resend transactional-email DNS records (send.granwatch.app MX/SPF, resend._domainkey DKIM) untouched throughout — notification emails never at risk. konstand@granwatch.app mailbox active; hello@granwatch.app created as an alias on it; live send/receive test passed (Gmail → hello@ → landed in konstand@ inbox). Webmail (mail.zoho.com) fully working. **Root-caused why macOS Mail.app kept saying "unable to verify account/password" no matter what: Zoho's own account settings say IMAP "is not available for your account" — the free plan is web-only, no IMAP/POP, regardless of password/app-password/server settings.** Cheapest fix = upgrade to Zoho Mail Lite (~R14/user/mo) or Workplace Standard (~R42/user/mo); alternative = just use mail.zoho.com and skip Mail.app. Awaiting Konna's call. Full detail: `DNS-BACKUP-cloudflare-2026-08-17.md` in this folder. Previous: 2026-08-14 — **🌍 FULL-APP i18n LIVE: the ENTIRE user-facing app now speaks all 8 languages (en/af/nl/fr/de/es/pt-BR/fil) — `744edca` (acquisition path + picker) + `2150f9f` (everything else: ElderProfile, Settings, Care, Counters, Gran+ modals, CreateElder, Notifications, Account, native sign-in, StatusRing, dates). No more mixed-language UX (Konna's Aug 14 report → fixed same day). Zero store updates needed.** iOS 1.0.1 (13) LIVE since Aug 11. ⚠️ LAUNCH-BLOCKING: LS webhook secret still broken in Railway (quotes).*

## Current state
- **🇱🇺 LUXEMBOURG DISTRIBUTOR REQUEST (Aug 25, pending Konna's decision):** inbound request for a regional distributor for Luxembourg. Tech: fully supported today, zero code changes — elders store ISO country codes (`elders.country`, "LU" valid in picker + Eurozone pricing list), gift partners resolve per-country (a LU supplier = one `countries: ["LU"]` entry in `giftPartners.ts`), and payer-country profit-split slices cleanly on LU. **⚠️ Before committing: confirm with Chani (chanispies@gmail.com) that the NL/BE territory was strictly Netherlands + Belgium and never framed as "Benelux"** — if Benelux was implied, LU is already hers. Languages fine (app speaks fr + de). Next: Konna confirms Chani's scope → then reply to the LU request / draft agent terms per the standing 50%-of-subscription-profit + 80/20 gifts model.
- **🚨 ANDROID GRAN+ PURCHASE BUG — FIXED + DEPLOYED same hour (`6889369`, Aug 20, verified live in prod bundle):** Konna's first real Android Gran+ purchase charged on Google Play but never unlocked in-app, and the app dead-ended on a "Session already exists" sign-in screen. THREE root causes: (1) Android recreated the activity while the Play payment sheet was up → the JS waiting to call `revenueCat.activateNative` was killed → server never told (money safe with Google/RC). (2) Backup webhook silently broken: client tagged purchases with `$elderId`/`$userId` but RevenueCat REJECTS custom `$`-prefixed attribute keys → webhooks arrived unroutable. (3) Relaunch landed on sign-in despite an active Clerk session; every sign-in attempt errored "Session already exists". Fixes: pending-purchase flag persisted before the sheet opens + `syncPendingPurchase()` heals orphans on next launch (new `usePurchaseHealer` hook on Dashboard/ElderProfile/ElderSettings); attributes renamed `elder_id`/`gw_user_id` (webhook reads both); SignInPage auto-skips when signed in + `session_exists` treated as success. **✅ Konna's purchase UNLOCKED via "Restore purchases" ("it worked") — the full Android billing chain (Play → RevenueCat → server → Gran+ flag) is now CONFIRMED end-to-end with real money; the "coming soon" fallback era is over. ✅ RC webhook VERIFIED (Aug 20): RC dashboard webhook "GranWatch Server" → https://granwatch.app/api/revenuecat/webhook, auth header value set, Production+Sandbox; endpoint probed live — 401 on bad/missing auth (proves Railway REVENUECAT_WEBHOOK_AUTH_HEADER is set + enforced); no auth-mismatch retries in server logs → original delivery accepted. Renewals/cancellations will sync.**
- **🌐 granwatch.com (old Manus app) — Konna redirected it to granwatch.app himself (Aug 20):** googling "granwatch" ranked the dead Manus granwatch.com first; Konna logged into the old Manus account and pointed all granwatch.com traffic → granwatch.app. **TODO: transfer the granwatch.com domain OUT of Manus custody** (get it unlocked + EPP/auth code from the Manus side, transfer into Konna's own Cloudflare account where granwatch.app DNS already lives, then recreate the 301 redirect as a Cloudflare redirect rule so it survives the move). Watch for the ICANN 60-day transfer lock and confirm the registrant contact email is one Konna controls.
- **🐛 "999 days" alert bug FIXED + deployed (`9e334fb`, Aug 18):** creating a new gran fired a whole-family RED push that night ("It's been 999 days — the whole family has been alerted", reported by Konna from new gran Ouma Fanny). Root cause: the 999 never-visited sentinel in `server/cron.ts` satisfied `isRed` immediately. Fix: visit-status red/nudge now require ≥1 logged visit (grey-until-first-log, same rule as counters); birthdays unaffected; first real visit resets the sentinel window cleanly. Typecheck clean, pushed to main → Railway auto-deploy. The admin-only `smartNotify.test` dispatcher shares the 999 pattern but is dev-tooling only — left as is.
- **📧 GranWatch email migration to Zoho Mail COMPLETE for webmail (Aug 18); Mail.app desktop client BLOCKED, needs Konna's decision:** clean new inbox stood up per Konna's request. DNS cutover (Cloudflare Email Routing → Zoho MX/SPF/DKIM) verified successful by Zoho itself; Resend transactional records confirmed untouched (Konna's explicit "don't mess up notification emails" instruction honored). konstand@granwatch.app is the primary mailbox; hello@granwatch.app is a live alias on it, confirmed working via an actual send/receive test; mail.zoho.com webmail fully functional. **Tried adding the account to macOS Mail.app — kept failing "unable to verify account name or password" no matter the password/app-specific-password tried. Root cause found by checking Zoho Mail's own Settings → Mail Accounts → IMAP tab: it explicitly says "This feature is not available for your account!" — Zoho's free plan is web-only, IMAP/POP is a paid-plan-only feature, so no password would ever have worked.** Cheapest paid tier with IMAP is Zoho Mail Lite (~R14/user/mo); Workplace Standard (~R42/user/mo) also qualifies. Alternative: skip Mail.app, use mail.zoho.com only (works today, free). Sent Konna the writeup with options — awaiting his call on whether to upgrade the plan.
- **🤝 BIZDEV (Aug 16):** (1) **Nadav Mankerman** (ex-CTO Sparko, IFA partner network, Israel) — warm intro via Einam; chatting on WhatsApp (⚠️ 24h disappearing messages); he probed traction + Android status; one-page Strategy Brief PDF delivered to Konna to send (curated: no agent %s, no insurance-exit; "long game" framed as strategic partners/acquirers, privacy-first). Israel = agent-pipeline fit; Hebrew positioned as "days of work, waiting for right partner". (2) **FloraQueen flower fallback ACTIVATED** (`10ab66c`): global (flowers-abroad) + IL-specific (flowers-israel) plain links, test-phase like Petal & Post; commission needs Konna to register at CJ Affiliate → then swap in tracking link + {SUBID} same day. Tel Aviv gran demo works NOW (profile country must be set to Israel). (3) **Petal & Post email draft** delivered (commission ask ~10%, tracked link/code, demo offer). (4) **Gunderson** parked warm (reading Killing Sacred Cows; re-entry via book insight). (5) DB truth: 35 users / 21 elders / 28 visits; organic strangers arriving weekly (Matthew Alberts Aug 16).
- **🚨 PLAY REJECTION SOLVED SAME DAY (Aug 14):** Google rejected bundle 3 (Broken Functionality — "app opens but keeps crashing"; evidence: dashboard worked, then "GranWatch keeps stopping"). **Root cause found by reproducing on the Mac's Android emulator + injecting native calls via WebView DevTools (no sign-in needed): stale `cordova-plugin-purchases` (leftover Manus experiment) shipped a conflicting RevenueCat native SDK → `NoSuchMethodError` in `PurchasesPlugin.configure` → hard crash the moment ANY gran profile opens (initRevenueCat).** iOS unaffected. Fix: removed the dead cordova plugin (`d32f093`), rebuilt, re-verified on emulator (configure OK, 0 fatals). **versionCode 4 / 1.0.1 signed AAB built + uploaded to Play production release with notes (crash fix + 8 languages), saved. ✅ SUBMITTED FOR REVIEW Aug 14 ~17:00 SAST (Konna's go, remote) — Play Console shows "Changes in review": Production 4 (1.0.1) + store listing + app content + countries. Typical review ≤7 days.** (the 11 = release + the standard app-content items re-sent after a rejection). No appeal needed (minor violation, resubmission suffices). Vitals/pre-launch had zero data — reviewer crashes don't report; the CDP-injection trick is the reusable diagnosis path.
- **🌍 i18n COMPLETE (`744edca` + `2150f9f`, Aug 14):** react-i18next; detector localStorage `gw-lang` → device language → en (pt-BR→pt). 8 locale files in `client/src/locales/` (~330 keys each) with culturally-correct gran terms (Ouma/Oma/Mamie/la abuela/Vovó/Lola). Chip `LanguagePicker` (NO dropdowns — iOS WKWebView bug) on Landing header + Account. Pass 2 applied via codemod (399 replacements across 15 files, verified live: all 8 languages in prod bundle). Dates via `i18n.language`. Deliberately still English: Admin panel, ElderSettings test-notifications dev block, Terms/Privacy legal pages, error-message fallbacks, aria-labels — cosmetic follow-up only. RTL (he/ar) deferred until Israel/Dubai agents prove demand; store LISTING translations separate task. ⚠️ Mac dev-env: shell has `NODE_ENV=production` → plain `npm install` PRUNES devDependencies. Always `NODE_ENV=development npm install --legacy-peer-deps --include=dev`.
- **🌙 Overnight Aug 12 (coach, while Konna slept):** (1) **SEO/GEO content moat LIVE** (`8b2ef1e`): server-rendered `/guides` (3 articles: visit frequency, guilt, sibling coordination), `/faq` (FAQPage schema — AI-assistant citable), `/compare/family-group-chat`, dynamic sitemap (9 URLs) + robots.txt; Landing footer links added; verified live in production. Copy follows Konna's rule: never limit who GranWatch is "for". (2) **WhatsApp-first invite sharing shipped** (B1 from Masterplan): green WhatsApp button with warm prefilled text on the invite-code card, native share sheet on share icons, Copy stays pure copy. (3) **RevenueCat Play credentials now ✓ VALID** (propagation done) — Android billing chain complete end-to-end; "coming soon" fallback will retire on first device test after Play approval. (4) **Play review still "In review"** (since Aug 8, day 4 — normal ≤7d). (5) BONUS: Play Console confirms **Android developer verification requirements MET** — Sep 30 deadline item resolved.
- **🎉 Custom counters LIVE (`cf64891`, Aug 10 night, Konna's "okay go"):** per-gran custom counters — any name (≤30) + emoji + interval (1–365d), horizontal DRAIN BAR under the status ring (ring colour banding; grey until first log). Scope: family (whole family sees/logs) or private 🔒 (owner only — never appears in family feed, never returned to other members, per Konna). Caps v1: 2 family/elder + 2 private/user. Tables `elderCounters`+`counterLogs` (migration **0018** — reviewed clean, 2 CREATE TABLEs only). `counterRouter` (list/logs/add/log/remove, Gran+ gated). Nightly cron: overdue push once per crossing via `lastNotifiedAt` (private→owner, family→notifyable members; push-only, deliberately quiet). UI: `CustomCounters.tsx` on ElderProfile — Log it 💚, expandable history, add dialog with chip buttons + native inputs ONLY (no Radix Select — the iOS WKWebView dropdown bug). Locked (free) elders see a dashed Gran+ teaser → upsell. SW bumped **gw-v5** (device test = open, force-close, open again). Widget emoji-instead-of-photo: deferred to native build 14. Free-tier teaser depth + visits-auto-count: open product decisions. **Aug 11: Konna device-tested iOS — "works great"; Edit button added next to Remove (`98ea9e6`, reuses the add dialog prefilled; scope fixed after creation; SW gw-v6).**
- **⚠️ SEC-01 webhook: Railway value is WRONG — quotes embedded.** LS Resend test → **401 Invalid signature**. Diagnosis (length-only check, no value exposed): Railway's LEMONSQUEEZY_WEBHOOK_SECRET is 42 chars, doesn't start with gw_ → the 40-char key was saved wrapped in quote marks. Fix command sent to Konna (`railway variables --set LEMONSQUEEZY_WEBHOOK_SECRET=<paste, NO quotes>`). After his re-run: LS webhook page → top delivery → Resend → expect 200. **Until fixed, LS subscription events (new web subscriptions, renewals, cancellations) are NOT reaching the server.**
- **🍋 Trial shortened 180d → 120d (`cce7a63`, Konna's call):** TRIAL_DAYS=120, new profiles only; existing trialEndsAt untouched. All "6 months" copy → "4 months" (both Gran+ modals + ElderSettings).
- **🤝 Care tab opened to all members (`cce7a63`):** any family member can now add routines AND appointments (was admin-only); delete stays admin-only. Routines now show a real log: 7-day strip + "Last: ✓ done by Name · date" from enriched `medications.list` (lastLog with logger names).
- **🔌 RevenueCat↔Play wiring DONE incl. JSON upload (Konna dragged it, "File saved").** "Credentials need attention" banner = normal during ≤36h Google propagation (from Aug 10 evening → check ~Aug 12). Then verify Subscribe on a real Android device; the `09ff285` "coming soon" fallback retires automatically once offerings return a price.
- **💰 CORRECTION (Konna, Aug 13): the LS "subscribers" were Konna's own test subscriptions — real external paying customers = 0.** Prior "2nd paying subscriber / MRR ZAR 158" claims are void. **PRICING PARITY SHIPPED (`f361986` + LS dashboard):** store currency ZAR→USD, Gran+ = $2.99/mo flat worldwide, all tiers resolve to $2.99, FAQ/copy updated. No real customers affected.
- **📧 T-14d trial warning SHIPPED (`14fbddc`):** nightly, whole family, once per elder (sentinel -40), warm copy. First real fires ~Dec 2026 (120d trials).
- **🔁 Play bundle 3 REJECTED Aug 14 (crash) → superseded by versionCode 4 (see top item). Old "in review" watch item closed.**
- **✅ Confirmed on device:** iOS badge lifecycle (clears properly), birthday picker (iOS native date input `89f7412`). `DEVICE-CHECKLIST.md` governs pre-release checks.
- **🚫 Hannah tester item cancelled permanently (Konna's call).**
- **🔗 INVITE LINKS iOS — FULLY UNBLOCKED (Aug 11).** Root causes: (1) cold-start deep links never handled → fixed via `getLaunchUrl()` (`fc7edfd`, live); (2) build 13 was never uploaded (ASC had only 1.0 (12); the Aug 4 "submitted" note was wrong). **Coach ran the entire build 13 pipeline remotely on Konna's Mac (his go): found+fixed Info.plists still hardcoding CFBundleVersion 12 (app+widget → 13), re-archived via xcodebuild, CLI upload failed (Xcode 26 providerId bug) → drove Xcode Organizer GUI instead → uploaded 14:01 → created 1.0.1 in ASC, What's-New pasted, build 13 attached, AUTOMATIC release + immediate rollout → SUBMITTED 14:36 — status "Waiting for Review"** (≤48h per Apple; demo account intact). After approval: both phones update → WhatsApp invite-link test should land on /join. Android bundle 3 (has the plugin) — same test on Samsung once Play approves. **CONFIRMED WORKING (Konna, TestFlight build 13, Aug 11 evening):** first test exposed an infinite reload loop (full-page navigation reset the dedup flag while getLaunchUrl kept re-firing) → fixed with a location-compare guard (`0a329b5`, verified live in prod bundle) → **"it works now"** — link opens app on Join a Family with code prefilled. Polish backlog (offered, not yet requested): single clean transition instead of double-load flash; invite landing with Gran's name/photo instead of bare code field.
- Web takes money (LS $2.99/mo worldwide, webhook currently broken — see above); iOS IAP $2.99/mo live; Android awaiting Play review (billing chain ready).

## Next actions (priority order)
1. **Webhook secret re-set (KONNA, 1 min)** → then Resend in LS → confirm 200. Blocks all web-subscription event processing.
2. **~Aug 12: check RC credentials status** (should turn green) → verify Android Subscribe on a real device.
3. **✅ BOTH STORES LIVE (iOS Aug 11, Android Aug 20) → LAUNCH-KIT WEEK IS NOW:** Konna's Samsung smoke test, granwatchapp FB/IG creation + social Post 1, the 10 WhatsApp launch-kit sends, Jaco second touch, retirement-village first touch (rep kit ready). Target 25 paying families. Watch: store-listing graphics change (feature graphic + framed screenshots) in review since Aug 20 — cosmetic, app stays live.
4. **After 1.0.1 releases:** both phones update → WhatsApp invite-link test (must land on /join). Same test on Samsung after Play approval.
5. **Device-test custom counters cross-account** (create family + private counter, log both, confirm private is invisible to wife's account + absent from feed). Counters themselves confirmed working on iOS by Konna Aug 11.
6. **i18n DONE for all user-facing screens (Aug 14).** Remaining cosmetic-only: aria-labels, error fallbacks, Terms/Privacy legal pages. Next i18n work when needed: store LISTING translations (Play anytime, iOS with next build) + RTL if Israel/Dubai agents sign. Then quarterly Agent Statement + agent one-pager, on Konna's go.
7. Backlog: T&Cs lawyer review; keystore backup; GEO playbook; widget emoji (native build 14); founding-family conversion offer before first trials expire (~Dec 2026); agent agreement one-pager template.

## Standing rules / watch items
- **🎯 STRATEGY (Konna, 2026-08-13): EXIT PLAY.** Goal = maximum users in minimum time → sell the app for millions. Margin optimisation is NOT the game. Territory agents (NL/BE/PT/ES/AU/IL/BR/+Dubai pipeline): geo-locked country rights, 50% of subscription PROFIT (after app hard costs) paid quarterly by payer country — NO agent invite-link attribution needed. Gifts: agent keeps 80% of local supplier profit, Konna invoices 20% quarterly (giftPartners.ts already country-resolved with per-click logs). Agent terms renegotiated at acquisition time.
- **💰 PRICING SETTLED (Konna, 2026-08-13): web ≈ store price parity, no ZAR for non-SA visitors. NEVER flag the web/iOS price gap again — struck from all lists.** iOS price-raise decision also dead.
- Secrets pass through human hands only — assistant tools refuse key files & secret values by design. Never reuse values that appeared in assistant logs. LS signing secret max 40 chars (`openssl rand -hex 20`).
- Client Trust stays OFF until review-account exception exists. Demo account d274bg@gmail.com in ASC notes — keep intact.
- SW is stale-while-revalidate: device tests need open → force-close → open AGAIN. Cache now gw-v5.
- Migration drift healed at 0017; 0018 reviewed+shipped. Never ship a generated migration unreviewed. Gate commits on tsc exit (use `set -o pipefail`, not `;`).
- Private counters/logs must NEVER surface in family feed or to non-owners (Konna, 2026-08-10).
- Never enter banking/financial credentials on Konstand's behalf — his action only.
